# 二、产品功能设计

## 2.1 产品功能结构

```
AI代码知识图谱平台
│
├── 项目管理
│   ├── 创建项目
│   ├── 代码仓库绑定
│   ├── 版本管理
│
├── 代码扫描
│   ├── Git仓库扫描
│   ├── 文件系统扫描
│   ├── 扫描历史
│
├── 代码分析
│   ├── AST解析
│   ├── 依赖分析
│   ├── API调用分析
│   ├── 模块关系分析
│
├── AI分析（新增）
│   ├── 代码语义理解
│   ├── 代码总结
│   ├── 风险分析
│   ├── 技术债分析
│
├── 知识图谱
│   ├── 类关系图
│   ├── 函数调用图
│   ├── 服务依赖图
│   ├── 数据流图
│
├── 代码文档生成
│   ├── 自动生成接口文档
│   ├── 自动生成模块文档
│   ├── 自动生成系统架构说明
│
└── AI模型管理
    ├── API Key 管理
    ├── 模型配置
    ├── 调用日志
```

---

# 三、AI 服务模块设计

统一使用：

**第三方模型 API**

---

# 四、AI 服务架构设计

```
           前端(Vue)
               │
               │
        API Gateway
               │
        AI Service Layer
        │           │
 Prompt Builder   Model Adapter
        │           │
        │      ┌─────────────┐
        │      │ OpenAI API  │
        │      │ Claude API  │
        │      │ DeepSeek API│
        │      │ GLM API     │
        │      └─────────────┘
        │
  AI Result Parser
        │
  Knowledge Graph Builder
        │
        Neo4j
```

---

# 五、AI调用流程

完整流程：

```
代码扫描
    ↓
AST解析
    ↓
提取代码结构
    ↓
构建AI Prompt
    ↓
调用第三方模型 API
    ↓
返回 AI 分析结果
    ↓
结构化解析
    ↓
存储到知识图谱
```

---

# 六、AI Prompt 设计

Prompt 模板：

```
你是一个高级软件架构师。

请分析以下代码并返回结构化JSON：

要求：

1 总结代码功能
2 识别类之间关系
3 识别函数调用关系
4 分析潜在风险
5 提取关键业务逻辑

代码：

{code}
```

返回格式：

```
{
  "summary": "",
  "classes": [],
  "functions": [],
  "dependencies": [],
  "risks": []
}
```

---

# 七、AI Service 技术设计

后端模块：

```
backend
│
├── ai
│   ├── ai_service.py
│   ├── prompt_builder.py
│   ├── model_adapter.py
│   ├── response_parser.py
│
├── analyzer
│   ├── ast_parser.py
│   ├── dependency_analyzer.py
│
├── graph
│   ├── graph_builder.py
│   ├── neo4j_service.py
```

---

# 八、模型 Adapter 设计

统一接口：

```
class ModelAdapter:

    def chat(self, prompt: str):
        pass
```

OpenAI实现：

```
class OpenAIAdapter(ModelAdapter):

    def chat(self, prompt):

        response = client.chat.completions.create(
            model="gpt-4o",
            messages=[
                {"role":"user","content":prompt}
            ]
        )

        return response.choices[0].message.content
```

Claude实现：

```
class ClaudeAdapter(ModelAdapter):

    def chat(self, prompt):

        response = client.messages.create(
            model="claude-3-sonnet",
            max_tokens=2000,
            messages=[
                {"role":"user","content":prompt}
            ]
        )

        return response.content
```

---

# 九、API Key 管理设计

数据库表

### ai_model_provider

```
id
name
base_url
description
```

---

### ai_model

```
id
provider_id
model_name
max_tokens
pricing
```

---

### ai_api_key

```
id
user_id
provider_id
api_key
status
created_at
```

---

# 十、知识图谱设计

图数据库：

**Neo4j**

节点类型：

```
Project
File
Class
Function
Module
Service
API
```

关系：

```
CALLS
DEPENDS_ON
IMPORTS
IMPLEMENTS
USES
```

示例：

```
ClassA -[CALLS]-> FunctionB
ModuleA -[DEPENDS_ON]-> ModuleB
```

---

# 十一、知识图谱可视化

前端：

Vue + ECharts Graph

图结构：

```
nodes
edges
```

示例：

```
{
 nodes:[
  {id:"ClassA"},
  {id:"ClassB"}
 ],
 edges:[
  {source:"ClassA",target:"ClassB"}
 ]
}
```

---

# 十二、数据库结构设计

核心表：

```
project
scan_task
code_file
code_class
code_function
code_dependency
ai_analysis
```

---

### ai_analysis

```
id
project_id
file_id
summary
risk_level
analysis_json
created_at
```

---
